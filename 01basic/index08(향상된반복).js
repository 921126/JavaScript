
    //for ~ in 향상된 for문
    // var arr = [1,2,3,4,5];
    
    // for(var i in arr){
    //     console.log(i); //인덱스값을 추출
    //     console.log(arr[i]); //배열 요소값 추출
    // }

    //객체에서는 키를 담는다.
    var obj ={a : '1', b : '2'};
    for(var i in obj){
        // console.log(i); i는 키값을 담는다.
        console.log(obj[i]); //객체의 값을 가져오기
    }

